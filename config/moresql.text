Create blog user
CREATE USER 'blogadmin'@'localhost' IDENTIFIED VIA mysql_native_password USING '***';GRANT ALL PRIVILEGES ON *.* TO 'blogadmin'@'localhost' REQUIRE NONE WITH GRANT OPTION MAX_QUERIES_PER_HOUR 0 MAX_CONNECTIONS_PER_HOUR 0 MAX_UPDATES_PER_HOUR 0 MAX_USER_CONNECTIONS 0;GRANT ALL PRIVILEGES ON `blog`.* TO 'blogadmin'@'localhost';

Run the blog.sql and it should set up some basics

install composer

then in the dir do a composer install

Makes sure to confiug the RBAC DB Config


